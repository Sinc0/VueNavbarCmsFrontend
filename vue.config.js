module.exports = {
    //default
    // publicPath: process.env.NODE_ENV === 'production' ? '/' : '/',
    
    //subdomain
    publicPath: process.env.NODE_ENV === 'production' ? '/subdomain/' : '/',
}